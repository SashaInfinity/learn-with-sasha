/** Shared constants for the learn-with-sasha UI module. */

/** The id of the empty dock box the chat home renders; SashaStage projects the
 *  3D character into this element by measuring its bounding rect every frame. */
export const SASHA_DOCK_ID = 'sasha-dock';
